.main__messages__message{data: {id: message.id}}
  %p.main__messages__message--talker
    = message.user.name
  %p.main__messages__message--date
    = message.created_at.strftime("%Y/%m/%d(%a) %H:%M:%S")
  - if message.content.present?
    %p.main__messages__message--text
      = message.content
  - if message.image.present?
    %p.main__messages__image
      = image_tag message.image.url, class: 'main__messages__message__image--image'